## Description
Currently, any nested field IDs that are created as of the `delta.icebergCompatV2` protocol feature are not written into the generated Parquet files as part of the data writes. This PR adds support.

## How was this patch tested?
Unit and integration tests using the Parquet data files generated by Delta-Spark

## Does this PR introduce _any_ user-facing changes?
No