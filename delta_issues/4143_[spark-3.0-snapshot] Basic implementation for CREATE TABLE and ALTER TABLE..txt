Basic implementation for CREATE TABLE and ALTER TABLE.

Note that because of some known interactions with Spark, not all the tests quite pass right now. All of the newly added ones to test the DDL functionality pass.